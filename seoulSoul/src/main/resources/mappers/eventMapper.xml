<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.multi.seoulsoul.event.model.dao.EventDAO">
    <insert id="insertEvent" parameterType="eventDTO">
        INSERT INTO EVENT (title, content, start_date, end_date, views, created_date)
        VALUES (#{title}, #{content}, #{startDate}, #{endDate}, #{views}, CURRENT_TIMESTAMP)
    </insert>
</mapper>